import styled, { css } from 'styled-components';

export const Wrapper = styled.div`
    ${() => css`
        margin: 0 auto;
        max-width: 1280px;
        padding-left: 24px;
        padding-right: 24px;
    `}
`;
